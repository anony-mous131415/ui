import { CampaignObjectivePipe } from './campaign-objective.pipe';

describe('CampaignObjectivePipe', () => {
  it('create an instance', () => {
    const pipe = new CampaignObjectivePipe();
    expect(pipe).toBeTruthy();
  });
});
