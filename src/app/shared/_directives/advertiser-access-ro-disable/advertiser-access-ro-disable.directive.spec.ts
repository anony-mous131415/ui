import { AdvertiserAccessRoDisableDirective } from './advertiser-access-ro-disable.directive';

describe('AdvertiserAccessRoDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvertiserAccessRoDisableDirective(null , null);
    expect(directive).toBeTruthy();
  });
});
