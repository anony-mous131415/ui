import { DisableDemoDirective } from './disable-demo.directive';

describe('DisableDemoDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableDemoDirective(null , null);
    expect(directive).toBeTruthy();
  });
});
