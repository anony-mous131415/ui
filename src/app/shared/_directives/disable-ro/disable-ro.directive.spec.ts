import { DisableRoDirective } from './disable-ro.directive';

describe('DisableRoDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableRoDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
