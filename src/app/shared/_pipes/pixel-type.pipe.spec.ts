import { PixelTypePipe } from './pixel-type.pipe';

describe('PixelTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PixelTypePipe();
    expect(pipe).toBeTruthy();
  });
});
